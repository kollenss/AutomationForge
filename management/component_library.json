{
  "categories": [
    {
      "id": "logic",
      "label": "Logic",
      "color": "#8b5cf6",
      "components": [
        {
          "type": "activate_scene",
          "label": "Activate Scene",
          "subtitle": "Scene control",
          "color": "#10b981",
          "icon": "▶",
          "display_param": "scene_name",
          "params": [
            {"key": "scene_name", "label": "Scene", "type": "scene_select", "default": ""}
          ],
          "inputs": [
            {"key": "trigger", "label": "Trigger", "description": "Activates the named scene when any signal arrives"}
          ],
          "outputs": []
        },
        {
          "type": "deactivate_scene",
          "label": "Deactivate Scene",
          "subtitle": "Scene control",
          "color": "#ef4444",
          "icon": "⏹",
          "display_param": "scene_name",
          "params": [
            {"key": "scene_name", "label": "Scene", "type": "scene_select", "default": ""}
          ],
          "inputs": [
            {"key": "trigger", "label": "Trigger", "description": "Deactivates the named scene when any signal arrives"}
          ],
          "outputs": []
        },
        {
          "type": "terminal_gate",
          "label": "Terminal Gate",
          "subtitle": "Floor 2 terminal",
          "color": "#0ea5e9",
          "icon": "⌨",
          "params": [
            {"key": "url",      "label": "Terminal URL",    "type": "text",     "default": "http://localhost:8080"},
            {"key": "password", "label": "Override Code",   "type": "password", "default": ""}
          ],
          "inputs": [
            {"key": "enable",  "label": "Enable",  "description": "Enables keyboard input on the terminal"},
            {"key": "disable", "label": "Disable", "description": "Disables keyboard input and resets to waiting screen"}
          ],
          "outputs": [
            {"key": "solved", "label": "Code Correct", "description": "Fires when the correct override code is entered"}
          ]
        },
        {
          "type": "on_scene_start",
          "label": "On Scene Start",
          "subtitle": "Scene event",
          "color": "#10b981",
          "icon": "🚀",
          "params": [],
          "inputs": [],
          "outputs": [
            {"key": "signal", "label": "Signal", "description": "Fires once when the scene is activated"}
          ]
        },
        {
          "type": "combo_lock",
          "label": "Combo Lock",
          "subtitle": "Rotary combination",
          "color": "#dc2626",
          "icon": "🔐",
          "display_param": "name",
          "params": [
            {
              "key": "code",
              "label": "Code (4 numbers 0-99, comma-separated)",
              "type": "text",
              "default": "10,20,30,40"
            },
            {
              "key": "name",
              "label": "Label",
              "type": "text",
              "default": "vault"
            }
          ],
          "inputs": [
            {"key": "enable",    "label": "Activate",        "description": "Starts the lock — connect from an RFID Auth or other trigger"},
            {"key": "delta",     "label": "Encoder Step",    "description": "Receives turn steps from a Rotary Encoder (+1 right, -1 left)"},
            {"key": "test_code", "label": "Confirm (Click)", "description": "Locks the current digit and advances phase — same effect as reversing direction"}
          ],
          "outputs": [
            {"key": "current_value", "label": "Current Count",  "description": "The current dial count (0-99) for the active phase"},
            {"key": "display_8",     "label": "8-Digit Text",   "description": "8-character string for a MAX7219 display — two digits per phase"},
            {"key": "digit_locked",  "label": "On Target",      "description": "Fires when the dial count matches the target number for the current phase"},
            {"key": "unlocked",      "label": "Code Correct",   "description": "Fires when all four locked digits match the configured code"},
            {"key": "failed",        "label": "Code Wrong",     "description": "Fires when the locked digits do not match the configured code"}
          ]
        },
        {
          "type": "rfid_auth",
          "label": "RFID Auth",
          "subtitle": "Card access gate",
          "color": "#6366f1",
          "icon": "🔑",
          "display_param": "name",
          "params": [
            {
              "key": "valid_uids",
              "label": "Valid UIDs (comma-separated)",
              "type": "text",
              "default": ""
            },
            {
              "key": "name",
              "label": "Label",
              "type": "text",
              "default": "RFID Gate"
            }
          ],
          "inputs": [
            {"key": "card_read", "label": "Card UID", "description": "Receives a card UID string — connect from an RFID Reader"}
          ],
          "outputs": [
            {"key": "authorized", "label": "Card Accepted", "description": "Fires when the scanned UID is in the valid UIDs list"},
            {"key": "denied",     "label": "Card Rejected", "description": "Fires when the scanned UID is not in the valid UIDs list"}
          ]
        },
        {
          "type": "password_lock",
          "label": "Password Lock",
          "subtitle": "Text input gate",
          "color": "#8b5cf6",
          "icon": "🔒",
          "params": [
            {"key": "password",     "label": "Password",     "type": "password", "default": ""},
            {"key": "max_attempts", "label": "Max Attempts", "type": "number",   "default": 3}
          ],
          "inputs": [
            {"key": "attempt", "label": "Password Input", "description": "Receives a password string to check against the configured password"}
          ],
          "outputs": [
            {"key": "unlocked",   "label": "Password Correct",       "description": "Fires when the submitted password matches"},
            {"key": "locked_out", "label": "Max Attempts Reached",   "description": "Fires when the maximum number of failed attempts is exceeded"}
          ]
        },
        {
          "type": "sequence",
          "label": "Sequence Gate",
          "subtitle": "Order-based trigger",
          "color": "#8b5cf6",
          "icon": "🔗",
          "params": [
            {"key": "length",        "label": "Steps",        "type": "number",  "default": 4},
            {"key": "reset_on_fail", "label": "Reset on fail","type": "boolean", "default": true}
          ],
          "inputs": [
            {"key": "step", "label": "Step Trigger", "description": "Each trigger is recorded as the next step in the sequence"}
          ],
          "outputs": [
            {"key": "complete", "label": "Sequence Complete", "description": "Fires when all required steps are triggered in the correct order"},
            {"key": "failed",   "label": "Wrong Order",       "description": "Fires when a step arrives out of the expected order"}
          ]
        },
        {
          "type": "timer",
          "label": "Timer",
          "subtitle": "Countdown",
          "color": "#8b5cf6",
          "icon": "⏱",
          "params": [
            {"key": "duration_s", "label": "Duration (s)", "type": "number",  "default": 60},
            {"key": "auto_start", "label": "Auto Start",  "type": "boolean", "default": false}
          ],
          "inputs": [
            {"key": "start", "label": "Start", "description": "Starts the countdown from the configured duration"},
            {"key": "reset", "label": "Reset", "description": "Stops and resets the timer back to the configured duration"}
          ],
          "outputs": [
            {"key": "tick",    "label": "Tick (each second)", "description": "Fires once per second while the timer is running"},
            {"key": "expired", "label": "Time Up",            "description": "Fires when the countdown reaches zero"}
          ]
        },
        {
          "type": "set_value",
          "label": "Set Value",
          "subtitle": "Fixed output value",
          "color": "#8b5cf6",
          "icon": "📌",
          "display_param": "value",
          "params": [
            {"key": "value", "label": "Value", "type": "text", "default": ""}
          ],
          "inputs": [
            {"key": "trigger", "label": "Trigger", "description": "Any incoming signal triggers the output"}
          ],
          "outputs": [
            {"key": "out", "label": "Value Out", "description": "Emits the configured value"}
          ]
        },
        {
          "type": "note",
          "label": "Note",
          "subtitle": "Design annotation",
          "color": "#8b5cf6",
          "icon": "📝",
          "params": [
            {"key": "text", "label": "Text", "type": "text", "default": ""}
          ],
          "inputs":  [],
          "outputs": []
        }
      ]
    }
  ]
}
