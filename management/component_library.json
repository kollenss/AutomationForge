{
  "categories": [
    {
      "id": "logic",
      "label": "Logic",
      "color": "#8b5cf6",
      "components": [
        {
          "type": "console_log",
          "label": "Console Log",
          "subtitle": "Debug output",
          "color": "#64748b",
          "icon": "📜",
          "display_param": "label",
          "params": [
            {"key": "label", "label": "Label", "type": "text", "default": "log", "description": "Name shown on the card and in the log line. E.g. door-check."}
          ],
          "inputs": [
            {"key": "value", "label": "Value", "description": "Logs whatever arrives here"}
          ],
          "outputs": [
            {"key": "out", "label": "Pass-through", "description": "Forwards the same value so you can chain logs"}
          ]
        },
        {
          "type": "if_else",
          "subtitle": "Conditional branch",
          "color": "#8b5cf6",
          "icon": "⚖",
          "display_param": "operand",
          "params": [
            {
              "key": "operator",
              "label": "Condition",
              "type": "select",
              "default": "equals",
              "options": [
                {"value": "equals",      "label": "= equals"},
                {"value": "contains",    "label": "contains"},
                {"value": "starts_with", "label": "starts with"},
                {"value": "ends_with",   "label": "ends with"},
                {"value": "greater_than","label": "> greater than"},
                {"value": "less_than",   "label": "< less than"}
              ],
              "description": "How the incoming value is compared against 'Compare to'. greater/less than compare as numbers; the rest as text."
            },
            {"key": "operand",        "label": "Compare to",      "type": "text",   "default": "",
             "description": "The value to compare the input against. E.g. OP-0987 for text, or 5 for greater/less than."},
            {"key": "case_sensitive", "label": "Case sensitive",  "type": "select", "default": "0",
             "options": [{"value": "0", "label": "No"}, {"value": "1", "label": "Yes"}],
             "description": "Whether text comparisons care about upper/lowercase. E.g. No treats ABC and abc as equal."}
          ],
          "inputs": [
            {"key": "value", "label": "Value", "description": "The value to test — connect from any output"}
          ],
          "outputs": [
            {"key": "then", "label": "Then (true)",  "description": "Fires with the input value when the condition is true"},
            {"key": "else", "label": "Else (false)", "description": "Fires with the input value when the condition is false"}
          ]
        },
        {
          "type": "activate_scene",
          "label": "Activate Scene",
          "subtitle": "Scene control",
          "color": "#10b981",
          "icon": "▶",
          "display_param": "scene_name",
          "params": [
            {"key": "scene_name", "label": "Scene", "type": "scene_select", "default": "", "description": "Which scene to activate when a signal arrives."}
          ],
          "inputs": [
            {"key": "trigger", "label": "Trigger", "description": "Activates the named scene when any signal arrives"}
          ],
          "outputs": []
        },
        {
          "type": "deactivate_scene",
          "label": "Deactivate Scene",
          "subtitle": "Scene control",
          "color": "#ef4444",
          "icon": "⏹",
          "display_param": "scene_name",
          "params": [
            {"key": "scene_name", "label": "Scene", "type": "scene_select", "default": "", "description": "Which scene to deactivate when a signal arrives."}
          ],
          "inputs": [
            {"key": "trigger", "label": "Trigger", "description": "Deactivates the named scene when any signal arrives"}
          ],
          "outputs": []
        },
        {
          "type": "terminal_gate",
          "label": "Web App Bridge",
          "subtitle": "HTTP integration",
          "color": "#0ea5e9",
          "icon": "⌨",
          "params": [
            {"key": "url",      "label": "App URL",       "type": "text",     "default": "http://localhost:8080",
             "description": "Base URL of the external web app GameForge controls. E.g. http://localhost:8080."},
            {"key": "password", "label": "Override Code", "type": "password", "default": "",
             "description": "Optional code sent to the app on Enable; leave blank to use the app's own configured code."}
          ],
          "inputs": [
            {"key": "enable",  "label": "Enable",  "description": "Enables keyboard input on the terminal"},
            {"key": "disable", "label": "Disable", "description": "Disables keyboard input and resets to waiting screen"}
          ],
          "outputs": [
            {"key": "success", "label": "Successful Outcome", "description": "Fires when the app reports a successful outcome"},
            {"key": "failure", "label": "Unsuccessful Outcome", "description": "Fires when the app reports a failed outcome"}
          ]
        },
        {
          "type": "on_scene_start",
          "label": "On Scene Start",
          "subtitle": "Scene event",
          "color": "#10b981",
          "icon": "🚀",
          "params": [],
          "inputs": [],
          "outputs": [
            {"key": "signal", "label": "Signal", "description": "Fires once when the scene is activated"}
          ]
        },
        {
          "type": "combo_lock",
          "label": "Combo Lock",
          "subtitle": "Rotary combination",
          "color": "#dc2626",
          "icon": "🔐",
          "display_param": "name",
          "params": [
            {
              "key": "code",
              "label": "Code (4 numbers 0-99, comma-separated)",
              "type": "text",
              "default": "10,20,30,40",
              "description": "The 4 target numbers in order, each 0-99, comma-separated. E.g. 27,14,9,42."
            },
            {
              "key": "name",
              "label": "Label",
              "type": "text",
              "default": "vault",
              "description": "Display name shown on the card. E.g. vault."
            }
          ],
          "inputs": [
            {"key": "enable",    "label": "Activate",        "description": "Starts the lock — connect from an RFID Auth or other trigger"},
            {"key": "delta",     "label": "Encoder Step",    "description": "Receives turn steps from a Rotary Encoder (+1 right, -1 left)"},
            {"key": "test_code", "label": "Confirm (Click)", "description": "Locks the current digit and advances phase — same effect as reversing direction"}
          ],
          "outputs": [
            {"key": "current_value", "label": "Current Count",  "description": "The current dial count (0-99) for the active phase"},
            {"key": "display_8",     "label": "8-Digit Text",   "description": "8-character string for a MAX7219 display — two digits per phase"},
            {"key": "digit_locked",  "label": "On Target",      "description": "Fires when the dial count matches the target number for the current phase"},
            {"key": "unlocked",      "label": "Code Correct",   "description": "Fires when all four locked digits match the configured code"},
            {"key": "failed",        "label": "Code Wrong",     "description": "Fires when the locked digits do not match the configured code"}
          ]
        },
        {
          "type": "rfid_auth",
          "label": "RFID Auth",
          "subtitle": "Card access gate",
          "color": "#6366f1",
          "icon": "🔑",
          "display_param": "name",
          "params": [
            {
              "key": "valid_uids",
              "label": "Valid UIDs (comma-separated)",
              "type": "text",
              "default": "",
              "description": "Card UIDs that are accepted, comma-separated. Copy a UID from an RFID Reader's live scan. E.g. A1B2C3D4, 0F0E0D0C."
            },
            {
              "key": "name",
              "label": "Label",
              "type": "text",
              "default": "RFID Gate",
              "description": "Display name shown on the card. E.g. RFID Gate."
            }
          ],
          "inputs": [
            {"key": "card_read", "label": "Card UID", "description": "Receives a card UID string — connect from an RFID Reader"}
          ],
          "outputs": [
            {"key": "authorized", "label": "Card Accepted", "description": "Fires when the scanned UID is in the valid UIDs list"},
            {"key": "denied",     "label": "Card Rejected", "description": "Fires when the scanned UID is not in the valid UIDs list"}
          ]
        },
        {
          "type": "password_lock",
          "label": "Password Lock",
          "subtitle": "Text input gate",
          "color": "#8b5cf6",
          "icon": "🔒",
          "params": [
            {"key": "password",     "label": "Password",     "type": "password", "default": "",
             "description": "The code that unlocks, checked against the submitted text."},
            {"key": "max_attempts", "label": "Max Attempts", "type": "number", "min": 1, "max": 99, "default": 3,
             "description": "How many wrong tries before 'Max Attempts Reached' fires. E.g. 3."}
          ],
          "inputs": [
            {"key": "attempt", "label": "Password Input", "description": "Receives a password string to check against the configured password"}
          ],
          "outputs": [
            {"key": "unlocked",   "label": "Password Correct",       "description": "Fires when the submitted password matches"},
            {"key": "locked_out", "label": "Max Attempts Reached",   "description": "Fires when the maximum number of failed attempts is exceeded"}
          ]
        },
        {
          "type": "checklist",
          "label": "Checklist",
          "subtitle": "Steps in order",
          "color": "#8b5cf6",
          "icon": "✓",
          "params": [
            {"key": "length",        "label": "Number of Steps", "type": "number", "min": 1, "max": 6, "default": 3,
             "description": "How many steps must be completed in order, 1-6. Only that many Step inputs are used."},
            {"key": "reset_on_fail", "label": "Reset on wrong order", "type": "boolean", "default": true,
             "description": "If on, a step arriving out of order resets the checklist back to the start."}
          ],
          "inputs": [
            {"key": "step_1", "label": "Step 1", "description": "First thing that must happen"},
            {"key": "step_2", "label": "Step 2", "description": "Second — only accepted after Step 1"},
            {"key": "step_3", "label": "Step 3", "description": "Third — only accepted after Step 2"},
            {"key": "step_4", "label": "Step 4", "description": "Fourth — only accepted after Step 3"},
            {"key": "step_5", "label": "Step 5", "description": "Fifth — only accepted after Step 4"},
            {"key": "step_6", "label": "Step 6", "description": "Sixth — only accepted after Step 5"}
          ],
          "outputs": [
            {"key": "complete",     "label": "Complete",      "description": "Fires when all steps are done in the correct order"},
            {"key": "out_of_order", "label": "Out of Order",  "description": "Fires when a step arrives before it is expected — checklist resets"}
          ]
        },
        {
          "type": "timer",
          "label": "Timer",
          "subtitle": "Countdown",
          "color": "#8b5cf6",
          "icon": "⏱",
          "params": [
            {"key": "duration_s", "label": "Duration (s)", "type": "number", "min": 1, "default": 60,
             "description": "Countdown length in seconds. E.g. 60."},
            {"key": "auto_start", "label": "Auto Start",  "type": "boolean", "default": false,
             "description": "If on, the timer starts as soon as the scene activates — no Start signal needed."}
          ],
          "inputs": [
            {"key": "start", "label": "Start", "description": "Starts the countdown from the configured duration"},
            {"key": "reset", "label": "Reset", "description": "Stops and resets the timer back to the configured duration"}
          ],
          "outputs": [
            {"key": "tick",    "label": "Tick (each second)", "description": "Fires once per second while the timer is running"},
            {"key": "expired", "label": "Time Up",            "description": "Fires when the countdown reaches zero"}
          ]
        },
        {
          "type": "set_value",
          "label": "Set Value",
          "subtitle": "Fixed output value",
          "color": "#8b5cf6",
          "icon": "📌",
          "display_param": "value",
          "params": [
            {"key": "value", "label": "Value", "type": "text", "default": "",
             "description": "The value emitted on Value Out when triggered. E.g. red to drive an LED, or any text/number."}
          ],
          "inputs": [
            {"key": "trigger", "label": "Trigger", "description": "Any incoming signal triggers the output"}
          ],
          "outputs": [
            {"key": "out", "label": "Value Out", "description": "Emits the configured value"}
          ]
        },
        {
          "type": "note",
          "label": "Note",
          "subtitle": "Design annotation",
          "color": "#8b5cf6",
          "icon": "📝",
          "params": [
            {"key": "text", "label": "Text", "type": "text", "default": "", "description": "Free-text annotation shown on the canvas. Has no effect on the game."}
          ],
          "inputs":  [],
          "outputs": []
        }
      ]
    }
  ]
}
